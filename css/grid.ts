import { base } from "./base";

const cols = {
  s: 8,
  m: 8,
  l: 12,
  xl: 12,
};

const colGap = {
  s: base(2),
  m: base(2),
  l: base(2),
  xl: base(2),
};

const rowGap = {
  s: base(2),
  m: base(2),
  l: base(2),
  xl: base(2),
};

export default {
  cols,
  colGap,
  rowGap,
};
